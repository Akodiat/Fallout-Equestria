package death;

import java.util.HashMap;
import java.util.Map;

public class DeathBuilder {

	public static Map<DeathActions, AbstractDeathProcessor> build(){
		Map<DeathActions, AbstractDeathProcessor> deathActions = new HashMap<>();
		
		
		
		return deathActions;
	}
}
